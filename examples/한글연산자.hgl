// 한글 논리 연산자 예제 (V2 신기능)

변수 나이 = 20
변수 학생 = 참

나이 >= 19 그리고 학생 만약 {
    출력("성인 학생입니다")
}

나이 < 13 또는 나이 >= 65 만약 {
    출력("할인 대상입니다")
} 아니면 {
    출력("일반 요금입니다")
}

// 기존 &&, || 도 동작합니다
나이 >= 19 && 학생 만약 {
    출력("(기존 문법도 OK)")
}
