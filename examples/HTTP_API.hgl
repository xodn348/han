시도 {
    출력("API 호출 중...")
    변수 응답 = HTTP_포함("https://jsonplaceholder.typicode.com/todos/1")
    변수 할일 = 제이슨_파싱(응답)

    출력(형식("제목: {0}", 할일["title"]))
    출력(형식("완료: {0}", 할일["completed"]))
} 처리(오류) {
    출력(형식("API 오류: {0}", 오류))
}
